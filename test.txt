/
/index.php
Логотип
Каталог
Показать еще...
Бренд
Сбросить
Модель
В каталог

----------+++
при запомнить меня
создается coockie['remember'] (ID : login, ОС, браузер, IP-адрес)
["HTTP_USER_AGENT"]=> string(115) "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/75.0.3770.100 Safari/537.36"
помимо этого сессия с userId
при логауте удаляется

на каждой странице идет проверка либо сессии потом куки, если первой нет
если нет сессии, а есть кука, то она проверяется и если все ок, то создается сессия, если нет, кука удаляется и редирект на каталог
если есть сессия, получаются все данные о пользователе
----------+++
поработать с корзиной
возможно поменять слово на значок и выводить количество товаров онлайн
----------
сделать админку
----------
разрулить роли


questions
------------------
как лучше кодить?
1
$newBrand = $this->clear($_POST['newBrand']);
$this->brend->createBrend($newBrand);
$this->redirect('Новый бренд успешно добавлен!', 'brends');
2
$newBrand = $this->clear($_POST['newBrand']);
if ($this->brend->createBrend($newBrand)) {
	$this->redirect('Новый бренд успешно добавлен!', 'brends');
}